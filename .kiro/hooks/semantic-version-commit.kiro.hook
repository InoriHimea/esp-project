{
  "enabled": true,
  "name": "語義版本更新並提交",
  "description": "當 spec 任務完成後，根據語義自動更新版本號並提交代碼",
  "version": "1",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "askAgent",
    "prompt": "一個 spec 任務剛剛完成。請執行以下步驟：\n1. 分析本次任務的變更內容，根據語義化版本規範（Semantic Versioning）判斷版本類型：\n   - 重大破壞性變更 → 升級主版本號（MAJOR）\n   - 新增功能但向下相容 → 升級次版本號（MINOR）\n   - 錯誤修復或小改動 → 升級修訂號（PATCH）\n2. 找到專案中的版本號文件（package.json、platformio.ini 或其他版本配置文件），更新版本號。\n3. 使用 git 將所有變更的文件加入暫存區（git add），然後以繁體中文撰寫提交訊息，格式為：「類型(範圍): 簡短描述」，並執行 git commit。\n請確保提交訊息清楚描述本次任務完成的內容。"
  }
}